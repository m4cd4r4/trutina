# Backend connection (server-side only — NOT exposed to browser)
BACKEND_URL=http://localhost:3004
SHIELDAPI_KEY=your-api-key-here

# Auth
LOGIN_PASSWORD=change-me-in-production
SESSION_SECRET=generate-a-random-string-minimum-32-characters-long

# Gate (defaults to enabled if not set)
# SITE_GATE=false

# Email
RESEND_API_KEY=re_xxxx
